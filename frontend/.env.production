# Production build (npm run build) shu faylni o'qiydi.
# Drag-and-drop deploy'dan oldin quyidagi qatorni oching va backend'ning
# OCHIQ HTTPS manzilini yozing (oxirida / bo'lmasin), so'ng qayta build qiling.
# Netlify'ning o'zida build qilinsa, Dashboard'dagi VITE_API_URL bu fayldan ustun turadi.
# VITE_API_URL=https://sizning-backend-manzilingiz.onrender.com
